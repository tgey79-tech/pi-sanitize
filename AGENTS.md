## Agent skills

### Issue tracker

Issues and PRDs for this repo live as GitHub issues. Use the `gh` CLI for all operations. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles map directly to the default labels in this repo. See `docs/agents/triage-labels.md`.

### Domain docs

Use single-context domain docs at the repo root: `CONTEXT.md` plus `docs/adr/`. See `docs/agents/domain.md`.
